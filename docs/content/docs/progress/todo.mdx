---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

## 产品与代码质量治理

- 按[运维后台交互规范](/docs/progress/admin-interaction-guidelines)继续为渠道、日志和兑换码补齐列显隐与移动端摘要卡片；批量能力后续按真实运营需求扩展，不默认给危险写路径增加“跨全部筛选结果”操作。

- `web/src/pages/canvas/project.tsx` 已压到约 1380 行；项目恢复与持久化、patch history、live viewport、媒体弹窗和多选工具条均已迁出或收敛。后续按真实职责继续拆分节点内容 renderer、脚本/导演台/版本弹窗装配，避免新增功能重新堆回页面入口。
- 拆分 `backend/internal/service/provider.go` 的文本、图片、视频和音频协议执行器，保持统一出网、安全和错误语义。
- 分批统一页面空状态、加载状态、表单校验和错误提示；每批都做桌面端、移动端和明暗主题关键路径验证。

## 影视工作流后续打磨

- 时序驱动后续：当前先支持 6 秒、10 秒、15 秒和 30 秒模板快捷插入，后续可补语法高亮、时间点校验和按视频模型实际时长自动推荐模板。
- 首尾帧后续：当前已把首帧/尾帧映射为 Seedance 图片 role 和 `/videos` 的 `image_urls` 扩展字段，后续需要结合实际上游渠道回包确认不同反代的字段兼容性。

- 本地 Agent 接入后续：按[本地 Agent 接入规划](/docs/progress/local-agent-integration-plan)把 Claude Code CLI Adapter 升级为 Claude Agent SDK Adapter，并完善工具队列体验。
- 技能库私有能力：公开社区分类筛选与热门/高分/最新排行已接入，上传和评论仍需要 Updream 私有登录态与真实接口契约；如需完整复刻，必须先明确 OAuth、服务账号或用户授权方式，不能复用浏览器 Cookie 或猜测私有接口。
- 接口安全后续：把通用画布生成、任务中心手动任务和在线 Agent 会话进一步收敛到后端用户渠道或系统渠道；当前自定义渠道密钥进入任务队列后会立即静态加密，但浏览器到后端的请求仍必须依赖 HTTPS 保护。
