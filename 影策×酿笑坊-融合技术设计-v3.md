# 影策 × 酿笑坊 融合技术设计 v3

> 日期：2026-09-02 ｜ 本版性质：**定稿级技术设计**（GPT 话术：V1 技术设计）
> 版本史：
> v1 讨论稿 —— 酿笑坊侧系统总览（能力/契约/方案 A-D）
> v2 施工版 —— 补影策源码实测（四通道矩阵、shot_revisions 契约发现、dreamina_cli 修正）
> **v3 定稿版 —— 合并 GPT 架构谈话（五层架构/职责宪法/Hell Grind 三拆/四阶段），调和三处矛盾，落到 API+Spec+文件+Demo**
>
> 证据基础：影策接入材料包 + open-ai-canvas 源码精选（逐文件实测）+ 酿笑坊总览 v1 + 本轮 GPT 谈话全文。

---

## 0. 本版相对 v2 更新了什么

1. **采纳 GPT 五层架构与「职责宪法」**（§1），v2 的四通道矩阵降级为实现手段：MCP=运输层、HTTP=服务基座、SKILL.md=Agent 引导协议（§3）。
2. **Scene Spec 定为两层契约**（§4）：意图层（Creative Scene Spec，输入）+ 执行层（Shot Contract，输出）。GPT 谈话只画了意图层；执行层用我实测的 `shot_revisions` 表结构落地，两层之间的桥就是酿笑坊 Compiler。
3. **知识通道定位修正**：SKILL.md 不再承载方法论本体（v2 的 P0 知识包思路作废），改为「教 Agent 调用酿笑坊能力」的引导协议；方法论一律运行时经 Router 检索注入。理由：影策 runtime 硬预算（≤4 技能 × ≤32K 字符 × ≤3 链接文件）+ GPT「知识检索≠知识注入」原则。
4. **时序改双轨制**（§8）：知识轨照 GPT 顺序跑 S3–S7；基建轨（契约+API+MCP）立即并行。解决「GPT 说先 S3–S7 再接影策」vs「用户已拍板启动融合」的张力：**融合启动 ≠ 改影策 ≠ 灌知识**，Phase 1–2 影策侧零代码改动、零知识硬编码。
5. **Hell Grind 三拆正式化**（§6）：A 方法论→配方卡（命名表）、B 15 Prompt Block→Platform Adapter 字段体系（映射表）、C 生产经验→Dailies/评估飞轮（字段草案）。
6. **记录并调和 GPT 谈话内部的三处不一致**（§0.1），避免两套文档打架。
7. GPT 结尾提议由它产出「V1 技术设计」——**本文件即该产物**，且比 GPT 版多一层实测源码依据（GPT 看不到影策源码，本文件的文件路径均来自源码精选包实测）。

### 0.1 GPT 谈话三处内部矛盾的调和裁定

| # | GPT 原文矛盾 | v3 裁定 |
|---|---|---|
| 1 | §三说 Phase 1 不碰 `retrieve`，§十二又说第一批做 `route/retrieve/translate/validate/compile` | retrieve 拆两半：**确定性检索**（现有 embed 索引查配方阁，无模型冷启动）→ Phase 1 可用；**语义重组**（compose/ctx，触发模型）→ 后置 Phase 2+ |
| 2 | §三说第一阶段不碰 `gate`，§十二 Phase 1 返回体里又有 `gate` 字段，Phase 3 才上链路 | gate **端点**第一天就存在（verify_gate 是纯函数）；gate **强制拦截**才等到 Phase 3 挂上生成链路。返回体里的 gate 是「随单附检报告」，不拦截 |
| 3 | §十三 Phase 2 入口是 Skill Picker / @Skill，但架构上知识经 Router 检索 | 技能包内容=**调用协议**（教 Agent 用 nxf_* 工具），不是知识本体。知识本体永远在酿笑坊侧经 Router 流动 |

---

## 1. 职责宪法（GPT §十六，全文采纳，增补两行）

| 系统 | 核心职责 | 边界（不做的事） |
|---|---|---|
| **影策** | UI、项目、Canvas、Shot、Asset、Workflow、Task、Render、结果展示 | 不做方法论决策 |
| **影策 Native Skill** | 标准化创作工作流（33 个，不动） | 不迁移、不融合进酿笑坊 |
| **Canvas Agent / MCP** | 实际操作 Canvas / 项目 / 资产（41 工具） | 不直调 provider（AGENT_PROMPT 铁律） |
| **酿笑坊 Router** | 判断应该采用什么方法 | 不执行、不生成 |
| **酿笑坊 Recipe** | 沉淀可复用创作方法（749 卡） | 不做成 Skill |
| **酿笑坊 Compiler** | 把方法+意图编译成执行 Prompt | 不管状态 |
| **酿笑坊 Gate** | 质量/合规检查 | Phase 3 前不拦截 |
| **Prompt Library** | 优秀 Prompt 实验样本（INBOX→…→ARCHIVED） | S7 后才建完整系统 |
| **Hell Grind** | 方法论 + Platform Adapter + 生产经验（三拆后分别入库） | 不保留为独立 Skill |
| **Cangjie** | 离线知识蒸馏工厂（candidate→verified→promoted） | 不在线 |
| **Render Platform** | 真正生成视频/图片（经影策网关） | — |
| **Dailies** | 记录结果与迭代经验（评估飞轮） | — |
| **nxf 引导技能**（新增） | 教 Agent 何时/如何调用酿笑坊能力 | 不承载方法论本体 |
| **Shot Contract**（新增） | 意图与执行之间的数据契约（两层，§4） | 不是数据库、不是前端 |

五个核心概念一句话：**Skill=工作流，Recipe=方法，MCP=工具，Canvas=操作空间，Project=状态，Gate=质量控制。**

影策侧硬预算（实测，写进宪法当约束条件）：
- 创作页 linked-context 档案：**≤4 技能、≤32,000 字符、≤3 链接文件/技能**（`web/src/services/skill-runtime.ts` SKILL_RUNTIME_PROFILES）
- 影策已有完整数据模型：`projects / project_units / shots / shot_revisions / shot_artifacts / assets / asset_versions / workflow_*` —— **酿笑坊侧永不新建 NiangxiaofangProject/Shot/Asset 等镜像表，杜绝双事实源**

---

## 2. 五层架构（采纳 GPT §一，右侧标注实现）

```text
用户/导演
   ↓
①影策 UI 层（创作页/Skill Picker/@Skill/Canvas/项目/资产）      ← 现成，零改动
   ↓
②影策 Agent/MCP 层（理解意图、调工具、操作 Canvas/项目）        ← Canvas Agent 现成
   ↓                    ↓
③影策原生能力          酿笑坊 Ability Layer
   33 Native Skill       Router / Retrieve / Compile / Translate / Validate / Gate / Adapter
   （工作流）                          ↑
   ↓                    ┌─────────────┴─────────────┐
   │              MCP 运输层（nxf_* 工具）      HTTP 服务层（FastAPI /api/nxf/*）
   │              （Agent 路径专用）            （服务基座，MCP 只是它的薄壳）
   ↓                    ↓
④酿笑坊知识资产层（配方阁 749 卡 / 酒曲室 / Router / Prompt Library / Scene Spec / Hell Grind / Cangjie 产出）
   ↑
⑤离线知识炼制工厂（Cangjie：图书/案例/Prompt/视频经验 → candidate → verified → promoted）
```

**运输层裁定**（对 v2 四通道的收编）：
- **HTTP（原通道 C）是本体**：`D:\酿笑坊\_engine\yingce_bridge\service.py`（FastAPI，无状态）
- **MCP（原通道 B）是 HTTP 的 stdio 薄壳**：`yingce_bridge\mcp_server.py`，注册 `nxf_*` 工具，实现方式仿 `canvas-agent/src/mcp-server.ts`（stdio transport + 转发 POST）。Agent 路径从此可达
- **SKILL.md（原通道 A）降级为引导协议**：`yingce_bridge\skill\SKILL.md`，内容是「何时调 nxf_route/nxf_scene_compile、结果如何回写 project_create_or_update_shots」，≤32K 硬预算内富余
- **深度通道（原通道 D）**：保留为 Phase 3 的影策侧改动选项（§7）

---

## 3. nxf 引导技能（Phase 1 唯一进影策的东西）

```yaml
name: 酿笑坊能力路由
description: >
  当用户请求涉及分镜/提示词编译/镜头设计/方法论选型/质量校验时，先调 nxf_route
  获取方法建议，再调 nxf_scene_compile 产出镜头与提示词，用 project_create_or_update_shots
  回写影策，生成一律走 canvas_generate_*。不直接编 prompt，不绕过 Gate 报告。
# 体积预算：≤8K 字符（32K 的 1/4，留余量给 33 原生技能共存）
```

要点：
- 装载方式：md/zip 上传或 GitHub install（`POST /skills/install`，`skill_packages.go:698` 要求 SKILL.md 必须、references/scripts/assets 可选）
- 它是**协议**不是知识：稳定、小、几乎不用迭代；方法论更新全部发生在酿笑坊侧 Router，影策无感（这正是 GPT「用户感觉不到背后多了一个酿笑坊」的实现机制）
- 原型：`影策技能-蒸馏守门员-v2.0-三车道.md`（现成样板），v3 版本在其上做减法：删知识本体，留调用协议+少量样例卡引用

---

## 4. 两层契约（本文件核心增量）

GPT 的 Creative Scene Spec 是**输入**（导演意图）；我实测的 `shot_revisions` 是**输出**（落库执行）。两层之间的转换器就是酿笑坊 Compiler。**不另起炉灶，不自造字段。**

### 4.1 Layer 1 · CreativeSceneSpec（意图层，Agent/LLM 产出）

采纳 GPT §四 JSON，补 `spec_version` 和 `brief`（选平台/总时长/画幅）：

```json
{
  "spec_version": "1.0",
  "scene": { "goal": "…", "location": "…", "time": "night", "mood": "…" },
  "characters": [ { "role": "…", "identity": "…", "state": "…" } ],
  "visual": { "style": "…", "camera": "…", "lighting": "…" },
  "beats": [ { "start": 0, "end": 2, "action": "…" } ],
  "constraints": { "identity_lock": true, "continuity_lock": true, "positive_only": true },
  "brief": { "platform": "seedance", "total_seconds": 15, "aspect": "16:9", "use_case": "ecommerce" }
}
```

### 4.2 Layer 2 · ShotContract（执行层，Compiler 产出）

每个字段右侧是对齐的影策落库列（`shot_revisions` 实测 schema）与酿笑坊现有 shot dict 字段——**三向对齐，编译产物零转换直接落库**：

```json
{
  "shot": {
    "title": "…",                       // shots.title
    "shot_size": "CU",                  // shot_revisions.shot_size
    "camera_angle": "eye_level",        // shot_revisions.camera_angle
    "camera_movement": "slow_push_in",  // shot_revisions.camera_movement
    "duration_ms": 4000,                // shot_revisions.duration_ms
    "plot_description": "…",            // shot_revisions.plot_description
    "action": "…",                      // shot_revisions.action
    "dialogue": null,                   // shot_revisions.dialogue
    "action_beats": [ { "start_ms": 0, "end_ms": 1500, "action": "…" } ],  // shot_revisions.action_beats_json
    "continuity_notes": "…"             // shot_revisions.continuity_notes
  },
  "render": {
    "platform": "seedance",
    "image_prompt": "…",                // shot_revisions.image_prompt
    "video_prompt": "…",                // shot_revisions.video_prompt
    "negative_prompt": "…",             // shot_revisions.negative_prompt
    "adapter_blocks": { "location_map": "…", "physics": "…", "audio": "…" }  // Hell Grind 15 块的扩展槽（§6.B）
  },
  "gate": { "verdict": "PASS", "failures": [] }
}
```

### 4.3 回写链路（零影策代码改动）

```
nxf_scene_compile 返回 ShotContract[]
→ Agent 调 MCP 工具 project_create_or_update_shots（schema 见 canvas-agent/src/schemas.ts projectShotSchema）
→ 落 shot_revisions（duration_ms / image_prompt / video_prompt / … 全字段吻合）
→ 生成走 canvas_run_generation / project_start_workflow_step
```

**这就是 Phase 1 最小闭环的数据形态**：影策到影策，酿笑坊只是中转的大脑。

---

## 5. Niangxiaofang Ability API（HTTP 本体 + MCP 薄壳）

采纳 GPT §三的 `/api/nxf/` 前缀与六端点，补一个 Phase 1 组合端点：

| 端点 | 方法 | 入 | 出 | Phase 1 | 底层函数（酿笑坊实测） |
|---|---|---|---|---|---|
| `/api/nxf/route` | POST | SceneSpec, top_k | recipes[{name,score,why}], native_skills[], adapter | ✅ | skill_router + 扩充后的配方卡 front-matter（§6.D） |
| `/api/nxf/compile` | POST | SceneSpec, recipes? | ShotContract[], warnings, coverage | ✅ | 视频生产线 engine（编译/normalize） |
| `/api/nxf/translate` | POST | ShotContract[], platform | prompts[{shot_id, image/video/negative}] | ✅ | prompt_translator（纯函数【稳】） |
| `/api/nxf/validate` | POST | prompts | issues[{code,severity,field,fix}] | ✅ | 四闸中的确定性闸（禁词/结构/一致性） |
| `/api/nxf/style` | POST | ShotContract[], style_mix | ShotContract[] | ✅ | style_mixer（纯函数【稳】） |
| `/api/nxf/gate` | POST | prompt, shot, policy | verdict, failures | ✅（只报告不拦截） | verify_gate【稳】 |
| `/api/nxf/scene/compile` | POST | SceneSpec | §4 全家桶（selected_recipes + selected_native_skills + shots + prompts + warnings + gate） | ✅ 组合端点 | 上面 5 个的编排 |
| `/api/nxf/health` | GET | — | 版本/卡数/索引状态 | ✅ | — |

**Phase 1 明确不做**：compose / retrieve-语义层 / ctx（模型冷启动、subprocess 复杂），同 GPT §三。

MCP 工具面（`nfx_` 前缀，1:1 镜像）：`nxf_route / nxf_compile / nxf_translate / nxf_validate / nxf_style / nxf_gate / nxf_scene_compile`。注册方式仿 `dreamina-mcp.ts:29 registerTool(...)`；Agent 侧获得途径：Codex 本地配置加一条 mcpServer（`python D:\酿笑坊\_engine\yingce_bridge\mcp_server.py`，stdio），零影策改动。

### 5.1 Router 返回结构（GPT §十二要求的具体化）

```json
{
  "recipes": [
    { "name": "identity_anchor", "score": 0.92, "why": "brief.identity_lock=true 且双角色" }
  ],
  "native_skills": [ { "tag": "ecommerce", "suggest": "顶级波普视觉广告导演", "reason": "use_case=ecommerce" } ],
  "adapter": { "platform": "seedance", "blocks": ["SCENE_CONTEXT","CAMERA","…"] },
  "coverage": { "beats_mapped": 3, "beats_total": 3, "warnings": [] }
}
```

---

## 6. Hell Grind 三拆 · 落地规范

### A. 方法论 → 配方阁（Recipe 卡，可长期复用的知识）

| Hell Grind 原则 | 配方卡命名 | verify_state 起点 |
|---|---|---|
| Identity Anchoring | `character_identity_lock` | candidate |
| GEO spatial layout | `location_spatial_anchor` | candidate |
| First-second fixed blocking | `first_frame_blocking` | candidate |
| State-specific assets | `character_state_asset` | candidate |
| Positive-only phrasing | `positive_constraint_language` | candidate |
| Micro-action | `micro_action_timing` | candidate |

入库走 Cangjie 三车道（candidate → verified → promoted），**起点全部 candidate**，与 S3–S7 知识轨合流。

### B. 15 Prompt Block → Platform Adapter 字段体系（不是 Skill、不是 Recipe）

SCENE CONTEXT / ACTIVE REFERENCES / LOCATION MAP / FIRST FRAME / FORMAT / OPTICS / CAMERA / ACTION / PHYSICS / LIGHTING / AUDIO / ACTING / STYLE / QUALITY / POSITIVE CONSTRAINTS —— 全部变成 `adapter_blocks` 的槽位。每个平台一个 adapter：`compilers/h3.py、seedance.py、veo.py、kling.py`（酿笑坊现有 prompt_translator 重构而来）。ShotContract.camera/format/optics/action/lighting/style/quality 与现有 shot dict 重叠部分保持原字段，`location_map / physics / active_references` 为新增吸收槽。

### C. 生产经验 → Production Telemetry / Dailies（不是知识）

`retry / 64:1 / 连续红探针 / curation ratio / parallel editing` 等进入 Dailies 评估飞轮（Phase 4）：

```json
{
  "recipe": "identity_anchor",
  "model": "…", "platform": "…",
  "success_rate": 0.0, "retry_count_avg": 0, "failure_modes": [],
  "evidence": [ { "shot_id": "…", "date": "…", "verdict": "…", "diff": "…" } ]
}
```

**C 层数据留在酿笑坊 Dailies**，不进影策、不进配方阁。

### D. Recipe 卡 front-matter 扩充（GPT §七采纳；不另起 Router Registry）

酿笑坊现有 `skill_router.json / skill_registry.json` 降级为**派生索引**（构建时从卡面生成，单一事实源在卡）：

```yaml
name: identity_anchor
verify_state: candidate        # 现有
boundary: …                    # 现有
operational_form: …            # 现有
# ↓ v3 新增五字段（Router 的「什么时候用」语义）
inputs:  [character_ref, first_frame]
outputs: [identity_lock]
priority: [character_consistency]
conflicts: [identity_free_generation]
compatible: [headless_reference, first_frame_blocking]
```

---

## 7. 四阶段计划（GPT §十二–十五采纳，落地为双轨制）

```text
基建轨（立即，不依赖知识轨）              知识轨（照 GPT §十八顺序）
I0 前置治理（git 基线/依赖重生成/            S3 Router/Gate 数据验证
   路径参数化/venv 锁定）                   S4 Verification
I1 FastAPI 六端点+组合端点                  S5 Promotion
   两层契约冻结(OpenAPI)                    S6 真实生产测试
I2 MCP 薄壳 + nxf 引导技能包                S7 稳定闭环
I3 Phase 1 最小闭环 Demo
        ↓ 合流门
Phase 1 闭环用「已晋升」少量卡跑通管道（配方阁 verify_state=promoted 的子集）
        ↓
Phase 2 @native skill + nxf 引导技能 双技能协同（用户无感）
        ↓
Phase 3 Gate 上生成链路（§7.1）
        ↓
Phase 4 飞轮：Cangjie 炼卡→生产→评估→Dailies→改卡
```

**双轨纪律**（回应 GPT「别把未验证知识硬编码进影策」的警告）：
- API/契约/引导技能里**零知识硬编码**——所有方法论经 Router 数据面流动
- Phase 1 只用已晋升卡，证明的是**管道**不是知识质量
- 知识轨进度不因基建轨挤压（GPT §十八：S3–S7 是当前最有价值工作）

### 7.1 Phase 3 · Gate 上链路的三个接线选项

| 选项 | 影策改动 | 确定性 | 建议 |
|---|---|---|---|
| P3a Agent 侧：引导技能指示 Agent 在 `project_register_task_output` 前调 `nxf_gate` | 零 | 概率性（LLM 遵守度） | Phase 3 起步 |
| P3b web 插件：`yingce.plugin/v1`，仿 `prompt-optimizer.ts` manifest（contributes aiCapabilities） | 插件，无核心改动 | 较强 | Phase 3 收口 |
| P3c Go 后端：生成任务回调处挂 Gate 钩子 | 改后端 | 最强 | 视网关收口需求再定 |

（P3c 的具体挂点文件不在源码精选包内，需完整仓库后定位。）

---

## 8. 第一条端到端 Demo（GPT §十七「先证明一条链」的具体剧本）

**任务**：「做一个 15 秒珠宝广告，风格参考波普视觉」——刻意选 GPT 谈话用过的例子。

```text
1. 用户在影策创作页：@酿笑坊能力路由 帮我做一个15秒珠宝广告，风格波普
2. Agent 读引导技能 → nxf_route(SceneSpec)
   ← recipes: [产品特写递进, 高转化产品露出×positive_constraint_language]
   ← native_skills: [ecommerce 类（如「顶级波普视觉广告导演」）]
3. Agent 并 @顶级波普视觉广告导演（工作流层）
4. Agent → nxf_scene_compile(SceneSpec + platform=seedance)
   ← 3×ShotContract（4/5/6s，凑 15s；image/video/negative_prompt 齐备；gate=PASS 报告）
5. Agent → project_create_or_update_shots(3 shots)        ← 影策镜头表落库
6. 用户在 Canvas 确认 → canvas_run_generation
7. （Phase 3 才有）结果 → nxf_gate 复核 → PASS→shot_artifacts / FAIL→Retry
```

验收标准：步骤 5 之后，影策数据库 `shot_revisions` 三行记录的字段值 = ShotContract 原值（零转换）；全程影策侧 git 零改动。

---

## 9. 三个绝对不要（GPT §十七，原文采纳）+ 一条修正

1. ❌ 749 Recipe ≠ 749 Skill（影策 32K/4 技能预算必然爆）
2. ❌ 影策 33 Skill 不搬进酿笑坊（工作流资产留在影策）
3. ❌ 不先建「大一统 Creative OS」（一条链跑稳再扩）
4. ✅ 修正 GPT/材料包共享的一处事实错误：`dreamina_cli` **是**影策自实现的（`canvas-agent/src/modules/dreamina-mcp.ts:29`），Agent 路径工具链是通的——这让 Phase 2/3 比 GPT 谈话假设的更可行。

---

## 10. 待拍板决策（v2 五项的更新版）

| # | 决策 | 建议 |
|---|---|---|
| 1 | ~~P0 技能包内容：垂类 vs 整套~~ | **已由架构消解**：技能包=引导协议（小而稳），垂类知识走 Router |
| 2 | 配方卡 GitHub 托管仓（公/私） | 公开仓（MIT 同影策）+ 影策 install-from-GitHub 自动 sync |
| 3 | Phase 3 走 P3a→P3b 还是直接 P3c | 先 P3a 起步、P3b 收口（P3c 视网关收口需求） |
| 4 | nxf 服务层 LLM 供应商 | Phase 1–2 自带 key（全部端点纯函数，暂不需要 LLM） |
| 5 | 部署形态 | Phase 1–2 本机 127.0.0.1（与 Canvas Agent 同模式），云化后置 |
| 6 | **新**：确认双轨制时序（知识轨 S3–S7 不被挤压） | 需要用户口头确认 |

---

## 11. 证据索引（v2 保留 + v3 增补）

| 结论 | 证据 |
|---|---|
| dreamina_cli 影策自实现 | `canvas-agent/src/modules/dreamina-mcp.ts:29` |
| 32K/4技能/3文件预算 | `web/src/services/skill-runtime.ts` SKILL_RUNTIME_PROFILES |
| SKILL.md 必须、包结构可推断 | `backend/internal/service/skill_packages.go:698-729` |
| GitHub install + sync(SourceCommit) | `backend/internal/handler/skills.go` |
| shot_revisions 全字段 | `影策数据库schema.md`（SQLite 实测） |
| project_create_or_update_shots schema | `canvas-agent/src/schemas.ts` projectShotSchema |
| AGENT_PROMPT「生成必须走共享任务」 | `canvas-agent/src/config.ts:14` |
| 插件范式 yingce.plugin/v1 | `web/src/lib/plugins/builtin/prompt-optimizer.ts` manifest |
| MCP 注册范式 | `canvas-agent/src/modules/dreamina-mcp.ts` registerTool |
| GPT 架构/宪法/三拆/四阶段 | 本轮粘贴文本（GPT 谈话全文） |
| 酿笑坊纯函数清单 | 酿笑坊总览 v1（prompt_translator/skeleton_router/engine/verify_gate/style_mixer【稳】） |
